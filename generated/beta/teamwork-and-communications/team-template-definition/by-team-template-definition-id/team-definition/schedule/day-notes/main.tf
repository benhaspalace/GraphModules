# Generated by GraphForm. Regenerate from pinned sources; do not edit.
locals {
  typed_body = { for key, value in {
    "createdBy"     = var.created_by
    "dayNoteDate"   = var.day_note_date
    "draftDayNote"  = var.draft_day_note
    "@odata.type"   = var.odata_type
    "sharedDayNote" = var.shared_day_note
  } : key => value if value != null }
  body = merge({ for key, value in var.additional_properties : key => value if value != null }, local.typed_body)
}

resource "msgraph_resource" "this" {
  url                     = "teamTemplateDefinition/${urlencode(var.team_template_definition_id)}/teamDefinition/schedule/dayNotes"
  api_version             = "beta"
  update_method           = "PATCH"
  body                    = local.body
  ignore_missing_property = true

  response_export_values = {
    response = "@"
  }
}
